pre-commit:
  commands:
    format:
      run: nub run format --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
    lint:
      run: nub run lint --fix --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
