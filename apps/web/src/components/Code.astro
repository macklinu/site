---
import { Code as ExpressiveCode } from 'astro-expressive-code/components'

import * as Post from '~/lib/Post'

interface Props {
  node: typeof Post.Code.Type
}

const {
  node: { code, language, filename },
} = Astro.props
---

<ExpressiveCode code={code} lang={language} title={filename} />
